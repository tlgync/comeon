export interface IGame {
    categoryIds: number[];
    code: string;
    description: string;
    icon: string;
    name: string;
}

export interface ICategories {
    id: number;
    name: string;
}
